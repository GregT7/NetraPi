{
  "enabled": true,
  "segments_dir": "src/main/data/trips",
  "segment_seconds": 300,
  "ffmpeg_crf": 20,
  "note": "Trip segments buffer frames in RAM, then encode once per segment with ffmpeg H.264 (requires ffmpeg on PATH)."
}
